package org.acme.probe;

/**
 * Marker class: the runtime half of the probe extension carries no behaviour,
 * it exists so that applications have something to depend on.
 */
public final class PackagingProbe {
    private PackagingProbe() {
    }
}
